# build page

## 

after update/edit

```code
git add .
git commit -m 'xxx'
git push
cmd /C "set "GIT_USER=momolly1024" && npm run deploy"
```

## PAGES
`https://momolly1024.github.io/cs-master-note/`

